import React from 'react'
import PropTypes from 'prop-types'

const MissionHandler = props => {
  return (
    <div>MissionHandler</div>
  )
}

MissionHandler.propTypes = {}

export default MissionHandler